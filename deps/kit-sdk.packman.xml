<project toolsVersion="5.0">
  <!-- Intentionally not a packman-fetched dependency: this repo targets a pre-existing binary
       Isaac Sim install (see ../_build/${platform_target}/${config}/kit, symlinked by hand to
       /home/isaac/isaacsim/kit) so the extension's ABI matches that exact install instead of a
       separately-downloaded Kit build. -->
</project>
